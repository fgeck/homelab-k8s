---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-media
  namespace: test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  volumeName: pv-nfs-media
  storageClassName: nfs-csi-media-static
